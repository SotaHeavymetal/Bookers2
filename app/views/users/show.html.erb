<div class = "container">
 <div class = "row">

<h1>Books</h1>
  <table class = "table">
   <thead>
    <th></th>
    <th>Title</th>
    <th>Opinion</th>
    <!--<th colspan="3"></th>-->
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
       <td>
        <%= link_to user_path(book.user) do %>
           <div>
        <%= attachment_image_tag book.user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg", class:"mt-3 rounded-circle"; %>
        <% end %>
        </div>
        </td>
        <td><%= link_to book.title, book_path(book) %></td>
        <td><%= book.body %></td>
          <% end %>
      </tr>
  </tbody>
</table>
  <!--<div>-->
  <!-- <h3>%= @user.name %></h3>-->
  <!-- <h3>%= @user.profile_image_id %></h3>-->
  <!--</div>-->

  <!-- % @book.each do |book| %>-->
  <!-- <div>-->
  <!--  %= link_to book_path(@book) %>-->
  <!--   %= attachment_image_tag book, :image %>-->
  <!-- </div>-->
  <!-- % end %>-->
</div>
</div>
