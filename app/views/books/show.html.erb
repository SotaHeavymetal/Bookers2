<% flash.each do |message_type, message| %>
 <%= message %>
 <% end %>
 <div class="container">
   <div class = "row">
    <div class = "col-md-3">
    <%= render 'books/list', user: @user, book: @books %>
    </div>
     <div class = "col-md-8 offset-md-1">

      <body>
     <h2>Book detail</h2>
      <table class = "table">
       <tr>
        <td>
         <%= attachment_image_tag @user, :profile_image, :fill, 10, 10, fallback: "no_image.jpeg", class:"mt-3 rounded-circle"; %><br>
         <%= link_to @user.name, user_path(@user) %>
        </td>
        <td><%= link_to @book.title, book_path(@book) %></td>
        <td><%= @book.body %></td>
       <% if @book.user == current_user %>
        <td><%= link_to "Edit", edit_book_path(@book), class:"btn btn-sm btn-success" %></td>
        <td><%= link_to "Destroy", book_path(@book), class:"btn btn-sm btn-danger", method: :delete, "data-confirm" => "本当に消しますか？" %></td>
　　   <% end %>
　　　</tr>
　　 </table>
　　</body>
　　</div>
　　</div>
　　</div>