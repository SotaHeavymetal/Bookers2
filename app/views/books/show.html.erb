 <% flash.each do |message_type, message| %>
 <%= message %>
 <% end %>
 <body>
  <h2>Book detail</h2>
   <table>      
    <tr>
     <td><div class="col-2">
          <%= attachment_image_tag @user, :profile_image, :fill, 10, 10, fallback: "no_image.jpeg", class:"mt-3 rounded-circle"; %>
        </div></td>
     <td><%= @book.title %></td>
     <td><%= @book.body %></td>
     <% if @book.user_id == current_user.id %>
     <td><%= link_to "Edit", edit_book_path(@book) %></td>
     <td><%= link_to "Destroy", book_path(@book), method: :delete, data: {confirm: 本当に消しますか？} %></td>
　　　<% end %>
　　　